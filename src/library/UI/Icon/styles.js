import { StyleSheet } from 'react-native';
import { Color } from '../Theme/Color';

export default StyleSheet.create({
  icon: {
    width: 22,
    height: 20,
    tintColor: Color.GRAY_LIGHT,
  },
});
